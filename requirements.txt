Flask==3.0.0
requests==2.31.0
beautifulsoup4==4.12.2
google-generativeai==0.3.2
gunicorn==21.2.0
lxml==5.1.0
```
4. **"Commit new file"**

#### **Archivo 3: Procfile**

1. **"Add file"** → **"Create new file"**
2. Nombre: `Procfile` (sin extensión, tal cual)
3. Copia y pega:
```
web: gunicorn app:app --workers 2 --threads 4 --timeout 120
```
4. **"Commit new file"**

#### **Archivo 4: Crear carpeta templates**

1. **"Add file"** → **"Create new file"**
2. En el nombre escribe: `templates/index.html` (el slash crea la carpeta automáticamente)
3. Ahora necesitamos el HTML. ¿Quieres que te cree un HTML básico funcional?

---

## 🎨 HTML Básico (te lo creo ahora)

Antes de continuar con Render, dime:

**¿Qué estilo prefieres para la interfaz?**
- A) Minimalista y profesional (estilo legal/abogado)
- B) Moderno y colorido (estilo startup tech)
- C) Simple y funcional (solo lo necesario)

Mientras decides, te explico el siguiente paso...

---

## 🚀 PARTE 2: Conectar con Render.com (lo haremos después del HTML)

### Paso 1: Crear un nuevo servicio en Render

1. Ve a **Render.com** y haz login
2. En el dashboard, haz clic en **"New +"** (arriba a la derecha)
3. Selecciona **"Web Service"**

### Paso 2: Conectar GitHub

1. Si es tu primera vez, haz clic en **"Connect GitHub"**
2. Autoriza a Render para acceder a tus repositorios
3. Busca y selecciona el repositorio `consultor-marcas-impi` (o como lo llamaste)

### Paso 3: Configurar el servicio

1. **Name**: `consultor-marcas` (o el que quieras)
2. **Region**: Elige el más cercano (probablemente Oregon o Ohio)
3. **Branch**: `main` (o `master` si es viejo)
4. **Root Directory**: déjalo en blanco
5. **Runtime**: Selecciona **Python 3**
6. **Build Command**: 
```
   pip install -r requirements.txt
```
7. **Start Command**:
```
   gunicorn app:app
